# PowerShell legacy (archived)

`bb-*` modules (`BusBuddy-Development.psm1` and related) were removed. Use `dotnet` CLI and `.github/scripts/validate-ci-local.sh`. Remaining scripts here are historical samples only.


RuleName                            Severity     ScriptName Line  Message
--------                            --------     ---------- ----  -------
PSAvoidUsingWriteHost               Warning      Setup-Azur 16    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 17    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 24    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 28    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 35    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 42    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 45    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 53    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 58    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 63    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 70    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 76    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 77    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 86    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 91    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 92    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 96    File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 103   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 107   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 112   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 113   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 114   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 119   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 134   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 135   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 136   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 137   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 141   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 146   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 153   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 158   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 159   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 160   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 164   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 171   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 175   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 180   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 187   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 191   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 195   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 199   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 203   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 204   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 208   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 214   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 219   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 226   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 233   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 241   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 249   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 254   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 255   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 256   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 257   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 258   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 259   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 260   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 261   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 262   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 263   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 264   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 265   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 266   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 269   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Setup-Azur 274   File 'Setup-Azure-CLI-Database.ps1' uses Write-Host.
                                                 e-CLI-Data       Avoid using Write-Host because it might not work in
                                                 base.ps1         all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 4     File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 5     File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 8     File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 11    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 14    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 15    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 20    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 23    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 24    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 27    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 28    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 33    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 36    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 37    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 38    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 41    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 42    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 46    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 48    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 52    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 56    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 58    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 62    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 65    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 66    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 67    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 68    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 69    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Azure 70    File 'Test-Azure-CLI-Connection.ps1' uses
                                                 -CLI-Conne       Write-Host. Avoid using Write-Host because it might
                                                 ction.ps1        not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BuildFunct 58    File 'BuildFunctions.ps1' uses Write-Host. Avoid
                                                 ions.ps1         using Write-Host because it might not work in all
                                                                  hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BuildFunct 64    File 'BuildFunctions.ps1' uses Write-Host. Avoid
                                                 ions.ps1         using Write-Host because it might not work in all
                                                                  hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BuildFunct 156   File 'BuildFunctions.ps1' uses Write-Host. Avoid
                                                 ions.ps1         using Write-Host because it might not work in all
                                                                  hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BuildFunct 206   File 'BuildFunctions.ps1' uses Write-Host. Avoid
                                                 ions.ps1         using Write-Host because it might not work in all
                                                                  hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BuildFunct 207   File 'BuildFunctions.ps1' uses Write-Host. Avoid
                                                 ions.ps1         using Write-Host because it might not work in all
                                                                  hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 43    File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 44    File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 47    File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 100   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 107   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 108   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 110   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 112   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 115   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 118   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 122   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 123   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 124   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 127   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Enhanced-B 129   File 'Enhanced-Build-Output.ps1' uses Write-Host.
                                                 uild-Outpu       Avoid using Write-Host because it might not work in
                                                 t.ps1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 32    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 33    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 42    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 45    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 47    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 61    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 65    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 69    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 76    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 80    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 86    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 91    File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 105   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 108   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 113   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 120   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 124   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 130   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 135   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 141   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 144   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 153   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 157   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 163   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 168   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 179   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 182   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 187   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 192   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 197   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 201   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 204   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 209   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 215   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 219   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 222   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 227   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 234   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 235   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 240   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 241   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 242   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 245   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 247   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 252   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 254   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 256   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 257   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 259   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      bb-validat 260   File 'bb-validate-database.ps1' uses Write-Host.
                                                 e-database       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 42    File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 43    File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 46    File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 102   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 109   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 110   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 112   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 114   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 117   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 120   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 124   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 125   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 126   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 129   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      BusBuddy.B 131   File 'BusBuddy.BuildOutput.psm1' uses Write-Host.
                                                 uildOutput       Avoid using Write-Host because it might not work in
                                                 .psm1            all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 34    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 35    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 42    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 85    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 90    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 91    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 95    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 96    File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 104   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 124   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 128   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 131   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 137   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 139   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 149   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 158   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 162   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 166   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 168   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 173   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 177   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 179   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 183   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 185   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 190   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 239   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 246   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 250   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 251   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 254   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 255   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 261   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Initialize 262   File 'Initialize-BusBuddyTesting.ps1' uses
                                                 -BusBuddyT       Write-Host. Avoid using Write-Host because it might
                                                 esting.ps1       not work in all hosts, does not work when there is
                                                                  no host, and (prior to PS 5.0) cannot be suppressed,
                                                                  captured, or redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Cleanup-Fo 13    File 'Cleanup-FoulFiles.ps1' uses Write-Host. Avoid
                                                 ulFiles.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Cleanup-Fo 88    File 'Cleanup-FoulFiles.ps1' uses Write-Host. Avoid
                                                 ulFiles.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Cleanup-Fo 90    File 'Cleanup-FoulFiles.ps1' uses Write-Host. Avoid
                                                 ulFiles.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Cleanup-Fo 104   File 'Cleanup-FoulFiles.ps1' uses Write-Host. Avoid
                                                 ulFiles.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Cleanup-Fo 113   File 'Cleanup-FoulFiles.ps1' uses Write-Host. Avoid
                                                 ulFiles.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 7     File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 9     File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 14    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 18    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 20    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 21    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 24    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 29    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 34    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 40    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 42    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 47    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 49    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 54    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 56    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 63    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 68    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 70    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 74    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 76    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 82    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 87    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 91    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 94    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 95    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 98    File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 103   File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 130   File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 153   File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 157   File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Debug-DICo 158   File 'Debug-DIContainer.ps1' uses Write-Host. Avoid
                                                 ntainer.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 23    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 31    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 32    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 34    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 46    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 51    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      MinimalOut 52    File 'MinimalOutputCapture.ps1' uses Write-Host.
                                                 putCapture       Avoid using Write-Host because it might not work in
                                                 .ps1             all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 29    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 41    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 58    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 59    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 60    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 61    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 62    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 84    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 87    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 88    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 89    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 99    File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 100   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 103   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 104   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 105   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 109   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 110   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 111   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 114   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 177   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 178   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 179   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 184   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 185   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 188   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 194   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Runtime-Ca 195   File 'Runtime-Capture-Monitor.ps1' uses Write-Host.
                                                 pture-Moni       Avoid using Write-Host because it might not work in
                                                 tor.ps1          all hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 32    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 38    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 40    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 46    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 49    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 54    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 56    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 62    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 65    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 69    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 71    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 77    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 83    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 85    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 93    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 98    File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 103   File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 108   File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.
PSAvoidUsingWriteHost               Warning      Test-Route 112   File 'Test-RouteService.ps1' uses Write-Host. Avoid
                                                 Service.ps       using Write-Host because it might not work in all
                                                 1                hosts, does not work when there is no host, and
                                                                  (prior to PS 5.0) cannot be suppressed, captured, or
                                                                  redirected. Instead, use Write-Output,
                                                                  Write-Verbose, or Write-Information.


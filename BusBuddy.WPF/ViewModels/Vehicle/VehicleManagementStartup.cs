namespace BusBuddy.WPF.ViewModels.Vehicle;

/// <summary>Optional action when fleet management opens (e.g. MainWindow Add Bus shortcut).</summary>
public enum VehicleManagementStartup
{
    None,
    AddVehicle,
}

<?xml version="1.0" encoding="utf-8"?>
<!-- Fixed malformed root element — header normalized for Pretty XAML formatting and editor toolchains.
     WPF docs: https://learn.microsoft.com/dotnet/desktop/wpf/
     Syncfusion WPF docs: https://help.syncfusion.com/wpf/welcome-to-syncfusion-essential-wpf -->
<syncfusionshared:ChromelessWindow x:Class="BusBuddy.WPF.Views.Main.MainWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:views="clr-namespace:BusBuddy.WPF.Views.Schedule"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                             xmlns:syncfusionshared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
                             xmlns:route="clr-namespace:BusBuddy.WPF.Views.Route"
                             xmlns:viewsGE="clr-namespace:BusBuddy.WPF.Views.GoogleEarth"
                             mc:Ignorable="d"
                             Title="🚌 BusBuddy MVP - Student Route Management"
                             Height="900"
                             Width="1400"
                             WindowStartupLocation="CenterScreen"
                             WindowState="Maximized">
    <!-- Theme resources merged to enable DynamicResource-based theming -->
    <syncfusionshared:ChromelessWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BusBuddy.WPF;component/Resources/SyncfusionV30_Validated_ResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- All ButtonAdv styles are now loaded from the global resource dictionary -->
            <!-- Using canonical resource keys: BusBuddy.ButtonAdv.Primary, BusBuddy.ButtonAdv.Info, BusBuddy.ButtonAdv.Warning -->
        </ResourceDictionary>
    </syncfusionshared:ChromelessWindow.Resources>
    <!-- MVP-Ready UI with DockingManager for professional layout -->
    <!-- Trigger language server refresh -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource BusBuddy.Brush.Header.Background}"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Text="🚌 BusBuddy MVP - Transportation Management"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{DynamicResource BusBuddy.Brush.Text.Primary}"
                           HorizontalAlignment="Center" />
                <TextBlock Grid.Row="1"
                           Text="Students | Routes | Buses | Drivers - Monday Ready Demo"
                           FontSize="16"
                           Foreground="{DynamicResource BusBuddy.Brush.Text.Primary}"
                           Opacity="0.9"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0" />
                <!-- Navigation + Theme Selector -->
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,15,0,0">
                    <syncfusion:ButtonAdv Label="🧾 Eligibility PDF"
                                          Margin="0,0,18,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="EligibilityPdfButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Generate eligibility route PDF (district outside town, excludes Lamar)"
                                          AutomationProperties.Name="Generate Eligibility PDF" />
                    <syncfusion:ButtonAdv Label="Print Last PDF"
                                          Margin="0,0,25,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="PrintEligibilityPdfButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Print the last generated eligibility PDF (opens system print dialog)"
                                          AutomationProperties.Name="Print Eligibility PDF" />
                    <syncfusion:ButtonAdv Label="🌍 Map"
                                          Margin="0,0,18,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Info}"
                                          Click="MapButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Show Google Earth / Geospatial Map"
                                          AutomationProperties.Name="Map View Navigation" />
                    <syncfusion:ButtonAdv Label="Students"
                                          Margin="0,0,10,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="StudentsButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Manage student records and information"
                                          AutomationProperties.Name="Students Management" />
                    <syncfusion:ButtonAdv Label="🚌 Routes"
                                          Margin="0,0,10,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="RouteManagementButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Plan and manage transportation routes"
                                          AutomationProperties.Name="Route Management" />
                    <syncfusion:ButtonAdv Label="🚐 Buses"
                                          Margin="0,0,10,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="BusesButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Manage bus fleet and vehicle information"
                                          AutomationProperties.Name="Bus Management" />
                    <syncfusion:ButtonAdv Label="👨‍✈️ Drivers"
                                          Margin="0,0,10,0"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="DriversButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Manage driver information and schedules"
                                          AutomationProperties.Name="Driver Management" />
                    <syncfusion:ButtonAdv Label="📊 Reports"
                                          Padding="15,8"
                                          Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                          Click="ReportsButton_Click"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          ToolTip="Generate and view transportation reports"
                                          AutomationProperties.Name="Reports and Print Routes" />
                    <!-- Theme Selector & Quick Toggle -->
                    <StackPanel Orientation="Horizontal"
                                Margin="20,0,0,0"
                                VerticalAlignment="Center">
                        <TextBlock Text="Theme:"
                                   Foreground="{DynamicResource ButtonForegroundBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <syncfusion:ComboBoxAdv x:Name="ThemeSelector"
                                                Width="150"
                                                SelectedIndex="0"
                                                SelectionChanged="ThemeSelector_SelectionChanged"
                                                ToolTip="Select application theme (Syncfusion Fluent themes)">
                            <syncfusion:ComboBoxItemAdv Content="FluentDark" />
                            <syncfusion:ComboBoxItemAdv Content="FluentLight" />
                        </syncfusion:ComboBoxAdv>
                        <syncfusion:ButtonAdv Label="🌙"
                                              Width="38"
                                              Margin="8,0,0,0"
                                              Padding="4"
                                              ToolTip="Switch to FluentDark"
                                              Click="DarkThemeButton_Click"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Primary}" />
                        <syncfusion:ButtonAdv Label="☀️"
                                              Width="38"
                                              Margin="4,0,0,0"
                                              Padding="4"
                                              ToolTip="Switch to FluentLight"
                                              Click="LightThemeButton_Click"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Info}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
        <!-- Main Content Area with DockingManager -->
        <syncfusion:DockingManager Name="MainDockingManager"
                                   UseDocumentContainer="True"
                                   PersistState="True"
                                   UseNativeFloatWindow="True"
                                   Grid.Row="1">
            <!-- Students Panel -->
            <ContentControl x:Name="StudentsPane"
                            syncfusion:DockingManager.Header="📚 Students"
                            syncfusion:DockingManager.State="Dock"
                            syncfusion:DockingManager.SideInDockedMode="Left"
                            syncfusion:DockingManager.DesiredWidthInDockedMode="350"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Margin="15"
                      Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <syncfusion:SfDataGrid Grid.Row="0"
                                           x:Name="StudentsGrid"
                                           ItemsSource="{Binding Students}"
                                           AutoGenerateColumns="False"
                                           AllowEditing="False"
                                           AllowSorting="True"
                                           AllowFiltering="True"
                                           AllowTriStateSorting="True"
                                           SelectionMode="Single"
                                           EnableDataVirtualization="True"
                                           GridLinesVisibility="Both"
                                           HeaderLinesVisibility="Both"
                                           Background="{DynamicResource BusBuddy.Brush.DataGrid.Background}"
                                           AutomationProperties.Name="Students Data Grid"
                                           ToolTip="List of all students with their information">
                        <syncfusion:SfDataGrid.Columns>
                            <syncfusion:GridTextColumn HeaderText="Student #"
                                                       MappingName="StudentNumber"
                                                       Width="100" />
                            <syncfusion:GridTextColumn HeaderText="Name"
                                                       MappingName="StudentName"
                                                       Width="140" />
                            <syncfusion:GridTextColumn HeaderText="Grade"
                                                       MappingName="Grade"
                                                       Width="60" />
                            <syncfusion:GridTextColumn HeaderText="Address"
                                                       MappingName="HomeAddress"
                                                       Width="180" />
                        </syncfusion:SfDataGrid.Columns>
                    </syncfusion:SfDataGrid>
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                Margin="0,15,0,0">
                        <syncfusion:ButtonAdv Label="➕ Add Student"
                                              Margin="0,0,10,0"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                              Click="AddStudent_Click"
                                              ToolTip="Add a new student to the system"
                                              AutomationProperties.Name="Add New Student" />
                        <syncfusion:ButtonAdv Label="📝 Edit Student"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Info}"
                                              Click="EditStudent_Click"
                                              ToolTip="Edit selected student information"
                                              AutomationProperties.Name="Edit Selected Student" />
                    </StackPanel>
                </Grid>
            </ContentControl>
            <!-- Routes Panel -->
            <ContentControl x:Name="RoutesPane"
                            syncfusion:DockingManager.Header="🚌 Routes"
                            syncfusion:DockingManager.State="Dock"
                            syncfusion:DockingManager.SideInDockedMode="Right"
                            syncfusion:DockingManager.DesiredWidthInDockedMode="380"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Margin="15"
                      Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <syncfusion:SfDataGrid Grid.Row="0"
                                           x:Name="RoutesGrid"
                                           ItemsSource="{Binding Routes}"
                                           AutoGenerateColumns="False"
                                           AllowEditing="False"
                                           AllowSorting="True"
                                           AllowFiltering="True"
                                           AllowTriStateSorting="True"
                                           SelectionMode="Single"
                                           EnableDataVirtualization="True"
                                           GridLinesVisibility="Both"
                                           HeaderLinesVisibility="Both"
                                           Background="{DynamicResource BusBuddy.Brush.DataGrid.Background}"
                                           AutomationProperties.Name="Routes Data Grid"
                                           ToolTip="List of all transportation routes">
                        <syncfusion:SfDataGrid.Columns>
                            <syncfusion:GridTextColumn HeaderText="Route Name"
                                                       MappingName="RouteName"
                                                       Width="120" />
                            <syncfusion:GridTextColumn HeaderText="School"
                                                       MappingName="School"
                                                       Width="140" />
                            <syncfusion:GridTextColumn HeaderText="Description"
                                                       MappingName="Description"
                                                       Width="180" />
                            <syncfusion:GridDateTimeColumn HeaderText="Date"
                                                           MappingName="Date"
                                                           Width="100"
                                                           Pattern="ShortDate" />
                        </syncfusion:SfDataGrid.Columns>
                    </syncfusion:SfDataGrid>
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                Margin="0,15,0,0">
                        <syncfusion:ButtonAdv Label="🗺️ Optimize Routes"
                                              Margin="0,0,10,0"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Info}"
                                              Click="OptimizeRoutes_Click"
                                              ToolTip="Optimize route efficiency and timing"
                                              AutomationProperties.Name="Optimize Transportation Routes" />
                        <syncfusion:ButtonAdv Label="📄 Export Schedules"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                              Click="ExportSchedules_Click"
                                              ToolTip="Export route schedules to file"
                                              AutomationProperties.Name="Export Route Schedules" />
                    </StackPanel>
                </Grid>
            </ContentControl>
            <!-- Buses Panel -->
            <ContentControl syncfusion:DockingManager.Header="🚐 Buses"
                            syncfusion:DockingManager.State="Document"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Margin="15"
                      Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <syncfusion:SfDataGrid Grid.Row="0"
                                           x:Name="BusesGrid"
                                           ItemsSource="{Binding Buses}"
                                           AutoGenerateColumns="False"
                                           AllowEditing="False"
                                           AllowSorting="True"
                                           AllowFiltering="True"
                                           AllowTriStateSorting="True"
                                           SelectionMode="Single"
                                           EnableDataVirtualization="True"
                                           GridLinesVisibility="Both"
                                           HeaderLinesVisibility="Both"
                                           Background="{DynamicResource BusBuddy.Brush.DataGrid.Background}"
                                           AutomationProperties.Name="Buses Data Grid"
                                           ToolTip="List of all buses in the fleet">
                        <syncfusion:SfDataGrid.Columns>
                            <syncfusion:GridTextColumn HeaderText="Bus #"
                                                       MappingName="BusNumber"
                                                       Width="120" />
                            <syncfusion:GridTextColumn HeaderText="License"
                                                       MappingName="LicenseNumber"
                                                       Width="120" />
                            <syncfusion:GridTextColumn HeaderText="Make"
                                                       MappingName="Make"
                                                       Width="100" />
                            <syncfusion:GridTextColumn HeaderText="Model"
                                                       MappingName="Model"
                                                       Width="120" />
                            <syncfusion:GridNumericColumn HeaderText="Year"
                                                          MappingName="Year"
                                                          Width="80" />
                            <syncfusion:GridNumericColumn HeaderText="Capacity"
                                                          MappingName="SeatingCapacity"
                                                          Width="80" />
                        </syncfusion:SfDataGrid.Columns>
                    </syncfusion:SfDataGrid>
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                Margin="0,15,0,0">
                        <syncfusion:ButtonAdv Label="➕ Add Bus"
                                              Margin="0,0,10,0"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                              Click="AddBus_Click"
                                              ToolTip="Add a new bus to the fleet"
                                              AutomationProperties.Name="Add New Bus" />
                        <syncfusion:ButtonAdv Label="🔧 Maintenance"
                                              Margin="0,0,10,0"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Warning}"
                                              Click="Maintenance_Click"
                                              ToolTip="Schedule and track bus maintenance"
                                              AutomationProperties.Name="Bus Maintenance" />
                        <syncfusion:ButtonAdv Label="📊 Fleet Status"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Info}"
                                              Click="FleetStatus_Click"
                                              ToolTip="View current fleet status and availability"
                                              AutomationProperties.Name="Fleet Status Overview" />
                    </StackPanel>
                </Grid>
            </ContentControl>
            <!-- Drivers Panel -->
            <ContentControl syncfusion:DockingManager.Header="👨‍✈️ Drivers"
                            syncfusion:DockingManager.State="Document"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Margin="15"
                      Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <syncfusion:SfDataGrid Grid.Row="0"
                                           x:Name="DriversGrid"
                                           ItemsSource="{Binding Drivers}"
                                           AutoGenerateColumns="False"
                                           AllowEditing="False"
                                           AllowSorting="True"
                                           AllowFiltering="True"
                                           AllowTriStateSorting="True"
                                           SelectionMode="Single"
                                           EnableDataVirtualization="True"
                                           GridLinesVisibility="Both"
                                           HeaderLinesVisibility="Both"
                                           Background="{DynamicResource BusBuddy.Brush.DataGrid.Background}"
                                           AutomationProperties.Name="Drivers Data Grid"
                                           ToolTip="List of all drivers and their information">
                        <syncfusion:SfDataGrid.Columns>
                            <syncfusion:GridTextColumn HeaderText="Name"
                                                       MappingName="DriverName"
                                                       Width="150" />
                            <syncfusion:GridTextColumn HeaderText="License Type"
                                                       MappingName="DriversLicenceType"
                                                       Width="120" />
                            <syncfusion:GridMaskColumn HeaderText="Phone"
                                                       MappingName="DriverPhone"
                                                       Width="120"
                                                       Mask="(###) ###-####" />
                            <syncfusion:GridTextColumn HeaderText="Email"
                                                       MappingName="DriverEmail"
                                                       Width="200" />
                        </syncfusion:SfDataGrid.Columns>
                    </syncfusion:SfDataGrid>
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                Margin="0,15,0,0">
                        <syncfusion:ButtonAdv Label="➕ Add Driver"
                                              Margin="0,0,10,0"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                              Click="AddDriver_Click"
                                              ToolTip="Add a new driver to the system"
                                              AutomationProperties.Name="Add New Driver" />
                        <syncfusion:ButtonAdv Label="🚌 Assign Bus"
                                              Margin="0,0,10,0"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Info}"
                                              Click="AssignBus_Click"
                                              ToolTip="Assign a bus to the selected driver"
                                              AutomationProperties.Name="Assign Bus to Driver" />
                        <syncfusion:ButtonAdv Label="📅 Schedule"
                                              Padding="12,6"
                                              Style="{StaticResource BusBuddy.ButtonAdv.Primary}"
                                              Click="Schedule_Click"
                                              ToolTip="Manage driver schedules and assignments"
                                              AutomationProperties.Name="Driver Schedule Management" />
                    </StackPanel>
                </Grid>
            </ContentControl>
            <!-- Map Panel (Google Earth Integration) -->
            <ContentControl x:Name="MapPane"
                            syncfusion:DockingManager.Header="🌍 Map"
                            syncfusion:DockingManager.State="Document"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <viewsGE:GoogleEarthView />
                </Grid>
            </ContentControl>
            <!-- Route Assignment Panel -->
            <ContentControl syncfusion:DockingManager.Header="🗺️ Route Assignments"
                            syncfusion:DockingManager.State="Document"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <route:RouteAssignmentView />
                </Grid>
            </ContentControl>
            <!-- Unified Scheduler Panel (Sports + Activities) -->
            <ContentControl syncfusion:DockingManager.Header="📅 Scheduler"
                            syncfusion:DockingManager.State="Document"
                            Background="{DynamicResource BusBuddy.Brush.Panel.Background}">
                <Grid Background="{DynamicResource BusBuddy.Brush.Panel.Content}">
                    <views:UnifiedSchedulerView />
                </Grid>
            </ContentControl>
        </syncfusion:DockingManager>
        <!-- Status Bar (High Contrast) -->
        <Border Grid.Row="2"
                Padding="15,12">
            <Border.Resources>
                <!-- High-contrast brushes (dark theme defaults). If Fluent Light is applied these fallback to readable dark text. -->
                <SolidColorBrush x:Key="StatusBarBackgroundBrush"
                                 Color="#12161C" />
                <SolidColorBrush x:Key="StatusBarBackgroundBrushLight"
                                 Color="#F3F4F6" />
                <SolidColorBrush x:Key="StatusBarPrimaryTextBrush"
                                 Color="#FFFFFF" />
                <SolidColorBrush x:Key="StatusBarPrimaryTextBrushLight"
                                 Color="#111827" />
                <SolidColorBrush x:Key="StatusBarAccentTextBrush"
                                 Color="#FACC15" />
                <SolidColorBrush x:Key="StatusBarAccentTextBrushLight"
                                 Color="#065F46" />
            </Border.Resources>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background"
                            Value="{StaticResource StatusBarBackgroundBrush}" />
                    <Style.Triggers>
                        <!-- Simple theme fallback: if application background is light (approx), switch palette -->
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Background.(SolidColorBrush.Color)}"
                                     Value="#FFFFFFFF">
                            <Setter Property="Background"
                                    Value="{StaticResource StatusBarBackgroundBrushLight}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="🎯 MVP Ready: Students ✅ | Routes ✅ | Buses ✅ | Drivers ✅ | Reports ✅ | PowerShell Demo: bb-route-demo"
                           Foreground="{StaticResource StatusBarPrimaryTextBrush}"
                           VerticalAlignment="Center"
                           FontSize="14"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="1"
                           Text="✅ Monday Demo Ready"
                           Foreground="{StaticResource StatusBarAccentTextBrush}"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold" />
            </Grid>
        </Border>
    </Grid>
</syncfusionshared:ChromelessWindow>

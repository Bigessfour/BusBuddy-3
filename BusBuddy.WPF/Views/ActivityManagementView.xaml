<UserControl x:Class="BusBuddy.WPF.Views.ActivityManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Activity Management System"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="20,10"/>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="20">
            <TabItem Header="Activities">
                <syncfusion:SfDataGrid ItemsSource="{Binding Activities}"
                                       AutoGenerateColumns="False"
                                       AllowSorting="True">
                    <syncfusion:SfDataGrid.Columns>
                        <syncfusion:GridTextColumn HeaderText="Name" MappingName="ActivityName"/>
                        <syncfusion:GridTextColumn HeaderText="Date" MappingName="ActivityDate"/>
                    </syncfusion:SfDataGrid.Columns>
                </syncfusion:SfDataGrid>
            </TabItem>
            <TabItem Header="Schedule">
                <TextBlock Text="Schedule management coming soon"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>

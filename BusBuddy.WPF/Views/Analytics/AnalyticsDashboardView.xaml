<UserControl x:Class="BusBuddy.WPF.Views.Analytics.AnalyticsDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BusBuddy.WPF;component/Resources/SyncfusionV30_Validated_ResourceDictionary.xaml"/>
                <ResourceDictionary Source="/BusBuddy.WPF;component/Resources/ButtonAdvTextOnly.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource PrimaryBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="20,10,20,0" Padding="12"
                Background="{DynamicResource ContentBackgroundBrush}" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Analytics Dashboard" FontSize="24" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                <syncfusion:ButtonAdv Grid.Column="1"
                                      Label="Refresh"
                                      SizeMode="Normal"
                                      Command="{Binding RefreshCommand}"
                                      AutomationProperties.Name="Refresh analytics"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Margin="5"
                    Background="{DynamicResource ContentBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Fleet Performance" FontWeight="SemiBold" Margin="0,0,0,8"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <syncfusion:SfChart Grid.Row="1">
                        <syncfusion:SfChart.PrimaryAxis>
                            <syncfusion:CategoryAxis Header="Status"/>
                        </syncfusion:SfChart.PrimaryAxis>
                        <syncfusion:SfChart.SecondaryAxis>
                            <syncfusion:NumericalAxis Header="Buses"/>
                        </syncfusion:SfChart.SecondaryAxis>
                        <syncfusion:ColumnSeries Label="Fleet"
                                                 XBindingPath="Label"
                                                 YBindingPath="Value"
                                                 ItemsSource="{Binding FleetPerformance}"
                                                 ShowTooltip="True"
                                                 EnableAnimation="True">
                            <syncfusion:ColumnSeries.AdornmentsInfo>
                                <syncfusion:ChartAdornmentInfo ShowLabel="True"
                                                               AdornmentsPosition="Top"
                                                               SegmentLabelFormat="0"/>
                            </syncfusion:ColumnSeries.AdornmentsInfo>
                        </syncfusion:ColumnSeries>
                    </syncfusion:SfChart>
                </Grid>
            </Border>

            <Border Grid.Row="0" Grid.Column="1" Margin="5"
                    Background="{DynamicResource ContentBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Student Assignments" FontWeight="SemiBold" Margin="0,0,0,8"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <syncfusion:SfChart Grid.Row="1">
                        <syncfusion:SfChart.PrimaryAxis>
                            <syncfusion:CategoryAxis Header="Status"/>
                        </syncfusion:SfChart.PrimaryAxis>
                        <syncfusion:SfChart.SecondaryAxis>
                            <syncfusion:NumericalAxis Header="Students"/>
                        </syncfusion:SfChart.SecondaryAxis>
                        <syncfusion:ColumnSeries Label="Students"
                                                 XBindingPath="Label"
                                                 YBindingPath="Value"
                                                 ItemsSource="{Binding RouteEfficiency}"
                                                 ShowTooltip="True"
                                                 EnableAnimation="True">
                            <syncfusion:ColumnSeries.AdornmentsInfo>
                                <syncfusion:ChartAdornmentInfo ShowLabel="True"
                                                               AdornmentsPosition="Top"
                                                               SegmentLabelFormat="0"/>
                            </syncfusion:ColumnSeries.AdornmentsInfo>
                        </syncfusion:ColumnSeries>
                    </syncfusion:SfChart>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Margin="5"
                    Background="{DynamicResource ContentBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Maintenance Metrics" FontWeight="SemiBold" Margin="0,0,0,8"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <syncfusion:SfChart Grid.Row="1">
                        <syncfusion:SfChart.Legend>
                            <syncfusion:ChartLegend DockPosition="Bottom" Orientation="Horizontal"/>
                        </syncfusion:SfChart.Legend>
                        <syncfusion:PieSeries XBindingPath="Label"
                                              YBindingPath="Value"
                                              ItemsSource="{Binding MaintenanceMetrics}"
                                              ShowTooltip="True"
                                              EnableAnimation="True"
                                              LabelPosition="OutsideExtended"
                                              ExplodeIndex="0"
                                              ExplodeRadius="10">
                            <syncfusion:PieSeries.AdornmentsInfo>
                                <syncfusion:ChartAdornmentInfo ShowLabel="True"
                                                               SegmentLabelContent="Percentage"
                                                               ShowConnectorLine="True"
                                                               ConnectorHeight="20"/>
                            </syncfusion:PieSeries.AdornmentsInfo>
                        </syncfusion:PieSeries>
                    </syncfusion:SfChart>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Margin="5"
                    Background="{DynamicResource ContentBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Fuel Analytics (30 days)" FontWeight="SemiBold" Margin="0,0,0,8"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <syncfusion:SfChart Grid.Row="1">
                        <syncfusion:SfChart.Legend>
                            <syncfusion:ChartLegend DockPosition="Bottom" Orientation="Horizontal"/>
                        </syncfusion:SfChart.Legend>
                        <syncfusion:SfChart.PrimaryAxis>
                            <syncfusion:CategoryAxis Header="Period"/>
                        </syncfusion:SfChart.PrimaryAxis>
                        <syncfusion:SfChart.SecondaryAxis>
                            <syncfusion:NumericalAxis Header="Gallons"/>
                        </syncfusion:SfChart.SecondaryAxis>
                        <syncfusion:ColumnSeries Label="Gallons"
                                                 XBindingPath="Label"
                                                 YBindingPath="Value"
                                                 ItemsSource="{Binding FuelGallons}"
                                                 ShowTooltip="True"
                                                 EnableAnimation="True">
                            <syncfusion:ColumnSeries.AdornmentsInfo>
                                <syncfusion:ChartAdornmentInfo ShowLabel="True"
                                                               AdornmentsPosition="Top"
                                                               SegmentLabelFormat="0"/>
                            </syncfusion:ColumnSeries.AdornmentsInfo>
                        </syncfusion:ColumnSeries>
                        <syncfusion:ColumnSeries Label="Records"
                                                 XBindingPath="Label"
                                                 YBindingPath="Value"
                                                 ItemsSource="{Binding FuelRecords}"
                                                 ShowTooltip="True"
                                                 EnableAnimation="True">
                            <syncfusion:ColumnSeries.YAxis>
                                <syncfusion:NumericalAxis Header="Records" OpposedPosition="True"/>
                            </syncfusion:ColumnSeries.YAxis>
                            <syncfusion:ColumnSeries.AdornmentsInfo>
                                <syncfusion:ChartAdornmentInfo ShowLabel="True"
                                                               AdornmentsPosition="Top"
                                                               SegmentLabelFormat="0"/>
                            </syncfusion:ColumnSeries.AdornmentsInfo>
                        </syncfusion:ColumnSeries>
                    </syncfusion:SfChart>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Margin="20,0,20,12" Text="{Binding StatusMessage}"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>

        <Border Grid.RowSpan="3" Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <syncfusion:SfBusyIndicator IsBusy="{Binding IsLoading}"
                                        Header="{Binding StatusMessage}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="BusBuddy.WPF.Views.Fuel.FuelManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource ContentBackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BusBuddy.WPF;component/Resources/SyncfusionV30_Validated_ResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="⛽" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Fuel Management" FontSize="24" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <syncfusion:ButtonAdv Label="➕ Add Record" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="✏️ Edit" Command="{Binding EditCommand}" IsEnabled="{Binding CanEdit}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="🗑️ Delete" Command="{Binding DeleteCommand}" IsEnabled="{Binding CanDelete}" Margin="0,0,8,0"/>
            <Separator Margin="8,0"/>
            <syncfusion:ButtonAdv Label="📊 Export CSV" Command="{Binding ExportCommand}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="📈 Fuel Report" Command="{Binding ReportCommand}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="🔄 Reconciliation" Command="{Binding ReconciliationCommand}"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <syncfusion:SfDataGrid Grid.Column="0" Margin="0,0,10,0"
                                   ItemsSource="{Binding FuelRecords}"
                                   SelectedItem="{Binding SelectedFuelRecord, Mode=TwoWay}"
                                   AutoGenerateColumns="False"
                                   AllowSorting="True"
                                   AllowFiltering="True"
                                   SelectionMode="Single"
                                   SelectionUnit="Row">
                <syncfusion:SfDataGrid.Columns>
                    <syncfusion:GridDateTimeColumn MappingName="FuelDate" HeaderText="Date" Width="110"/>
                    <syncfusion:GridTextColumn MappingName="FuelLocation" HeaderText="Location" Width="120"/>
                    <syncfusion:GridNumericColumn MappingName="VehicleFueledId" HeaderText="Bus ID" Width="80"/>
                    <syncfusion:GridNumericColumn MappingName="VehicleOdometerReading" HeaderText="Odometer" Width="90"/>
                    <syncfusion:GridTextColumn MappingName="FuelType" HeaderText="Type" Width="80"/>
                    <syncfusion:GridNumericColumn MappingName="Gallons" HeaderText="Gallons" Width="80"/>
                    <syncfusion:GridNumericColumn MappingName="PricePerGallon" HeaderText="$/Gal" Width="80"/>
                    <syncfusion:GridNumericColumn MappingName="TotalCost" HeaderText="Total" Width="90"/>
                    <syncfusion:GridTextColumn MappingName="Notes" HeaderText="Notes" Width="*"/>
                </syncfusion:SfDataGrid.Columns>
            </syncfusion:SfDataGrid>

            <Border Grid.Column="1" Padding="12"
                    Background="{DynamicResource PrimaryBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Monthly MPG Trend" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <syncfusion:SfChart Grid.Row="1">
                        <syncfusion:SfChart.PrimaryAxis>
                            <syncfusion:DateTimeAxis IntervalType="Months"/>
                        </syncfusion:SfChart.PrimaryAxis>
                        <syncfusion:SfChart.SecondaryAxis>
                            <syncfusion:NumericalAxis/>
                        </syncfusion:SfChart.SecondaryAxis>
                        <syncfusion:LineSeries ItemsSource="{Binding FuelTrends}"
                                               XBindingPath="Period" YBindingPath="AvgMPG"
                                               EnableAnimation="True"/>
                    </syncfusion:SfChart>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="3" Margin="0,12,0,0" Text="{Binding StatusMessage}"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>

        <Border Grid.RowSpan="4" Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <syncfusion:SfBusyIndicator IsBusy="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>

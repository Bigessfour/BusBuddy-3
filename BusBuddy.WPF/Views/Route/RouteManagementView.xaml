<UserControl x:Class="BusBuddy.WPF.Views.Route.RouteManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="{DynamicResource FluentDarkBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
            <Button Content="Generate Schedule"
                    Command="{Binding GenerateScheduleCommand}"
                    Style="{DynamicResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
            <Button Content="View Map"
                    Command="{Binding ViewMapCommand}"
                    Style="{DynamicResource PrimaryButtonStyle}"/>
        </StackPanel>
        <syncfusion:SfDataGrid Grid.Row="1"
                              ItemsSource="{Binding Routes}"
                              AutoGenerateColumns="False"
                              AllowSorting="True"
                              AllowFiltering="True"
                              SelectionMode="Single"
                              ColumnSizer="Star"
                              ShowRowHeader="True"
                              Background="{DynamicResource FluentDarkBackgroundBrush}">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="RouteName" HeaderText="Route Name" Width="150"/>
                <syncfusion:GridTextColumn MappingName="RouteDescription" HeaderText="Description" Width="200"/>
                <syncfusion:GridTextColumn MappingName="Path" HeaderText="Path" Width="200"/>
                <syncfusion:GridTextColumn MappingName="BusNumber" HeaderText="Bus Number" Width="100"/>
                <syncfusion:GridTextColumn MappingName="VINNumber" HeaderText="VIN" Width="180"/>
                <syncfusion:GridTextColumn MappingName="AssignedStudents" HeaderText="Assigned Students" Width="250"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>
    </Grid>
</UserControl>

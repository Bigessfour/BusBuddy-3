<UserControl x:Class="BusBuddy.WPF.Views.Maintenance.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource ContentBackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BusBuddy.WPF;component/Resources/SyncfusionV30_Validated_ResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="🔧" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Fleet Maintenance" FontSize="24" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <syncfusion:ButtonAdv Label="Add Record" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="Save" Command="{Binding SaveCommand}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="Delete" Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
            <syncfusion:ButtonAdv Label="Refresh" Command="{Binding RefreshCommand}"/>
        </StackPanel>

        <syncfusion:SfDataGrid Grid.Row="2"
                               ItemsSource="{Binding Records}"
                               SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                               AutoGenerateColumns="False"
                               AllowSorting="True"
                               AllowFiltering="True"
                               AllowEditing="True"
                               SelectionMode="Single"
                               SelectionUnit="Row">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridDateTimeColumn MappingName="Date" HeaderText="Date" Width="110"/>
                <syncfusion:GridNumericColumn MappingName="VehicleId" HeaderText="Bus ID" Width="80"/>
                <syncfusion:GridNumericColumn MappingName="OdometerReading" HeaderText="Odometer" Width="100"/>
                <syncfusion:GridTextColumn MappingName="MaintenanceCompleted" HeaderText="Work" Width="160"/>
                <syncfusion:GridTextColumn MappingName="Vendor" HeaderText="Vendor" Width="140"/>
                <syncfusion:GridNumericColumn MappingName="RepairCost" HeaderText="Cost" Width="90"/>
                <syncfusion:GridTextColumn MappingName="Status" HeaderText="Status" Width="110"/>
                <syncfusion:GridTextColumn MappingName="Priority" HeaderText="Priority" Width="90"/>
                <syncfusion:GridTextColumn MappingName="Notes" HeaderText="Notes" Width="200"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>

        <TextBlock Grid.Row="3" Margin="0,12,0,0" Text="{Binding StatusMessage}"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>
    </Grid>
</UserControl>

<Window x:Class="BusBuddy.WPF.Views.Driver.DriverForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:syncfusionshared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.Wpf"
        mc:Ignorable="d"
        Title="Driver Information"
        Height="450" Width="600">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Driver Information"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Driver Name:" Margin="0,0,0,5"/>
            <syncfusion:SfMaskedEdit Text="{Binding DriverName, Mode=TwoWay}" Height="30" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="License Number:" Margin="0,0,0,5"/>
            <syncfusion:SfMaskedEdit Text="{Binding LicenseNumber, Mode=TwoWay}" Height="30" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Phone Number:" Margin="0,0,0,5"/>
            <syncfusion:SfMaskedEdit Text="{Binding DriverPhone, Mode=TwoWay}" Height="30" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,10">
            <TextBlock Text="Status:" Margin="0,0,0,5"/>
            <syncfusion:ComboBoxAdv Height="30" SelectedItem="{Binding Status, Mode=TwoWay}">
                <ComboBoxItem Content="Active"/>
                <ComboBoxItem Content="Inactive"/>
            </syncfusion:ComboBoxAdv>
        </StackPanel>

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <syncfusion:ButtonAdv Label="Save"
                    Width="80"
                    Height="35"
                    Margin="0,0,10,0"
                    Command="{Binding SaveCommand}"/>
            <syncfusion:ButtonAdv Label="Cancel"
                    Width="80"
                    Height="35"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>

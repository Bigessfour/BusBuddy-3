<UserControl x:Class="BusBuddy.WPF.Views.Driver.DriverScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource ContentBackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BusBuddy.WPF;component/Resources/SyncfusionV30_Validated_ResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"
                       Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBlock Text="Driver Schedule" FontSize="24" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <syncfusion:ButtonAdv Grid.Row="1" Label="Refresh" Command="{Binding RefreshCommand}"
                              HorizontalAlignment="Left" Margin="0,0,0,12"/>

        <syncfusion:SfScheduler Grid.Row="2"
                                ViewType="Week"
                                ItemsSource="{Binding Appointments}"
                                FirstDayOfWeek="Monday">
            <syncfusion:SfScheduler.AppointmentMapping>
                <syncfusion:AppointmentMapping
                    Subject="Subject"
                    StartTime="StartTime"
                    EndTime="EndTime"
                    Location="Location"
                    Notes="Notes"/>
            </syncfusion:SfScheduler.AppointmentMapping>
        </syncfusion:SfScheduler>

        <TextBlock Grid.Row="3" Margin="0,12,0,0" Text="{Binding StatusMessage}"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>
    </Grid>
</UserControl>

<UserControl x:Class="BusBuddy.WPF.Views.ActivityScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionshared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20">
            <TextBlock Text="Activity Schedule"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <syncfusion:ButtonAdv Label="🔄 Refresh"
                    Margin="20,0,10,0"
                    Padding="10,5"
                    Background="#3498DB"
                    Foreground="White"
                    BorderThickness="0"/>
            <syncfusion:ButtonAdv Label="➕ Add Activity"
                    Margin="10,0"
                    Padding="10,5"
                    Background="#27AE60"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>

        <!-- Content Section -->
        <Grid Grid.Row="1" Margin="20">
            <TextBlock Text="Activity Schedule View - MVP Phase"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="Gray"/>
        </Grid>
    </Grid>
</UserControl>


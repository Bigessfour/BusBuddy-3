<!--
    StudentForm.xaml — BusBuddy Student Entry/Edit Dialog
    Uses Syncfusion ChromelessWindow for modern, chromeless UI.
    All controls styled and sized for accessibility and data type clarity.
    Theming is applied via SfSkinManager in code-behind (FluentDark default, FluentLight fallback).
    ResourceDictionary merges project-wide Syncfusion styles and converters.
    See code-behind for theme logic and event handling.
    https://help.syncfusion.com/wpf/chromelesswindow/overview
-->
<syncfusion:ChromelessWindow x:Class="BusBuddy.WPF.Views.Student.StudentForm"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            mc:Ignorable="d"
                            Title="Student Form"
                            Height="600"
                            Width="800"
                            WindowStartupLocation="CenterScreen"
                            d:DesignHeight="600"
                            d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Student Information"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Form Content -->
        <StackPanel Grid.Row="1"
                    Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top">
            <!-- Student ID -->
            <StackPanel Orientation="Horizontal"
                        Margin="0,10">
                <TextBlock Text="Student ID:"
                           Width="120"
                           VerticalAlignment="Center"/>
                <syncfusion:SfTextBoxExt Text="{Binding StudentId, Mode=TwoWay, ValidatesOnDataErrors=True}"
                                         Width="200"
                                         HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Student Name -->
            <StackPanel Orientation="Horizontal"
                        Margin="0,10">
                <TextBlock Text="Student Name:"
                           Width="120"
                           VerticalAlignment="Center"/>
                <syncfusion:SfTextBoxExt Text="{Binding StudentName, Mode=TwoWay, ValidatesOnDataErrors=True}"
                                         Width="200"
                                         HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Additional fields can be added here based on ViewModel properties -->
            <!-- Example: Route or other student-related fields -->
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <syncfusion:ButtonAdv Content="Save"
                                  Command="{Binding SaveCommand}"
                                  Width="100"
                                  Margin="10,0"/>
            <syncfusion:ButtonAdv Content="Cancel"
                                  Command="{Binding CancelCommand}"
                                  Width="100"
                                  Margin="10,0"/>
        </StackPanel>
    </Grid>
</syncfusion:ChromelessWindow>

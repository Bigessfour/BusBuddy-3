<UserControl x:Class="BusBuddy.WPF.Controls.QuickActionsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionshared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <UserControl.Resources>
        <!-- Syncfusion ButtonAdv Style with FluentDark Theme Integration -->
        <Style x:Key="QuickActionButtonStyle" TargetType="syncfusion:ButtonAdv">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{DynamicResource AccentBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="SizeMode" Value="Normal"/>
            <Setter Property="IconHeight" Value="16"/>
            <Setter Property="IconWidth" Value="16"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Quick Actions" FontSize="16" FontWeight="Bold"
               HorizontalAlignment="Center" Margin="10"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
            <syncfusion:ButtonAdv Label="Add Vehicle"
                    Style="{StaticResource QuickActionButtonStyle}"
                    SizeMode="Normal"
                    SmallIcon="pack://application:,,,/Assets/Icons/vehicle-add.png"
                    Click="AddVehicle_Click"/>
            <syncfusion:ButtonAdv Label="Add Driver"
                    Style="{StaticResource QuickActionButtonStyle}"
                    SizeMode="Normal"
                    SmallIcon="pack://application:,,,/Assets/Icons/driver-add.png"
                    Click="AddDriver_Click"/>
            <syncfusion:ButtonAdv Label="Schedule Route"
                    Style="{StaticResource QuickActionButtonStyle}"
                    SizeMode="Normal"
                    SmallIcon="pack://application:,,,/Assets/Icons/route-schedule.png"
                    Click="ScheduleRoute_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
